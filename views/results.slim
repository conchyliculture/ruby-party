div id="dialog-form" title="Create new user"

javascript:
    var dialog, form;

    function addUser() {
        console.log("lol");
    }

    dialog = $( "#dialog-form" ).dialog({
      autoOpen: false,
      height: 300,
      width: 350,
      modal: true,
      buttons: {
        "Create an account": addUser,
        Cancel: function() {
          dialog.dialog( "close" );
        }
      },
      close: function() {
      }
    });
    form = dialog.find( "form" ).on( "submit", function( event ) {
      event.preventDefault();
      addUser();
    });

    $( ".addtopl" ).button().on( "click", function() {
        var lolid= $(this.data).data("lolid");
        push_to_playlist(lolid);
    });

    $( ".editinfo" ).button().on( "click", function() {
        $.ajax({
            url: "/dialog",
            data: "id="+$(this).data("lolid"),
            success: function(data){
                $("#dialog-form").html(data);
            }   
        });

        dialog
        .html('lolid', $(this).data('lolid'))
        .dialog( "open" );
    });


-@results.each do |res|
    div class="result"
        div class="thumb"
            -if res.has_key?(:cover)
                img src="#{res[:cover]}"  class="thumb"
        div class="link_title"
            | #{res[:title]} / #{res[:yid]} <a href="/videos/#{res[:file]}">(Download file)</a>
        div class="push_pl"
            button class="addtopl" data-lolid="#{res[:id]}" Push to current Playlist
        div class="edit"
            button class="editinfo" data-lolid="#{res[:id]}" Edit Info
        div class="bottom"

